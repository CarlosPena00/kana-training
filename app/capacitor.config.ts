import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.kanatraining.flashcards',
  appName: 'Kana Flashcards',
  webDir: 'dist',
};

export default config;
